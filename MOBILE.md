# Мобильная версия

## ✅ Реализованные возможности

### Адаптивный дизайн

**Поддерживаемые разрешения:**
- 📱 Мобильные (320px - 767px)
- 📱 Планшеты (768px - 1024px)
- 💻 Десктоп (1025px+)

### Мобильная навигация

**Гамбургер-меню:**
- Боковое меню с плавной анимацией
- Overlay для затемнения фона
- Автоматическое закрытие при выборе файла
- Свайп для закрытия (нативный жест)

**Хедер:**
- Компактный на мобильных (56px)
- Адаптивные кнопки
- Скрытие лишних элементов
- Truncate для длинных путей

### Адаптивные компоненты

**Layout:**
- Гамбургер-меню вместо sidebar
- Мобильная нижняя панель с названием файла
- Скрытие preview на мобильных (только editor)
- Полноэкранный граф

**FileTree:**
- Увеличенные touch targets (44x44px)
- Больше padding для удобства
- Overlay меню на мобильных
- Счетчик файлов

**Editor:**
- Адаптивный CodeMirror
- Оптимизированный размер шрифта
- Предотвращение zoom на iOS (font-size: 16px)

**Graph:**
- Адаптивные размеры canvas
- Touch controls (pinch to zoom)
- Подсказки для жестов
- Оптимизированный размер узлов

**Login:**
- Адаптивный padding
- Иконка приложения
- AutoComplete для форм
- Мобильная клавиатура

### Оптимизации

**Производительность:**
- Предотвращение pull-to-refresh
- Отключение bounce scroll
- Hardware acceleration для анимаций
- Оптимизация рендеринга

**UX улучшения:**
- Минимальный размер touch targets (44px)
- Предотвращение случайного zoom
- Адаптивные размеры шрифтов
- Break-word для длинных текстов

**Markdown preview:**
- Адаптивные заголовки (h1-h6)
- Горизонтальный скролл для таблиц
- Адаптивные изображения (max-width: 100%)
- Мобильные размеры кода

## 📱 Фавиконы и PWA

### Иконки

**Форматы:**
- SVG (любой размер) - /favicon.svg
- PNG 192x192 - /icon-192.png
- PNG 512x512 - /icon-512.png
- Apple Touch Icon

**Дизайн:**
- Фон #0C0F16
- Граф узлов #284CAC
- Минималистичный стиль
- Хорошо читается в любом размере

### Web App Manifest

```json
{
  "name": "Knowledge Base",
  "short_name": "KB",
  "theme_color": "#0C0F16",
  "background_color": "#000000",
  "display": "standalone"
}
```

**Возможности:**
- Установка на домашний экран
- Standalone режим (как нативное приложение)
- Кастомный цвет темы
- Иконки для всех платформ

### Meta теги

**Mobile optimization:**
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="theme-color" content="#0C0F16">
```

## 🎯 Breakpoints

```css
/* Mobile */
@media (max-width: 767px) {
  - Гамбургер меню
  - Одна колонка
  - Скрыт preview
  - Упрощенный header
}

/* Tablet */
@media (min-width: 768px) and (max-width: 1024px) {
  - Sidebar доступен
  - Split view для editor/preview
  - Полный header
}

/* Desktop */
@media (min-width: 1025px) {
  - Всё показано
  - Оптимальные размеры
}
```

## 🔧 CSS классы

**Tailwind breakpoints:**
- `sm:` - ≥640px (small)
- `md:` - ≥768px (medium)
- `lg:` - ≥1024px (large)
- `xl:` - ≥1280px (extra large)

**Примеры использования:**
```tsx
// Скрыть на мобильных
className="hidden md:flex"

// Разные размеры
className="text-sm md:text-base"

// Адаптивный padding
className="p-2 md:p-4"
```

## 📐 Touch targets

**Минимальные размеры:**
- Кнопки: 44x44px
- Ссылки: 44x44px
- Checkbox/Radio: 44x44px
- Input fields: минимум 44px высота

**Реализация:**
```css
@media (max-width: 768px) {
  button, a {
    min-height: 44px;
    min-width: 44px;
  }
}
```

## 🎨 Мобильные жесты

**Поддерживаемые жесты:**

**Граф:**
- Pinch to zoom (двумя пальцами)
- Drag to pan (один палец)
- Tap для выбора узла

**Меню:**
- Swipe right to open (нативный)
- Tap overlay to close
- Swipe left to close (нативный)

**Скролл:**
- Вертикальный скролл
- Горизонтальный для таблиц/кода
- Pull-to-refresh отключен

## 📱 Установка как PWA

### iOS (Safari)

1. Открыть в Safari
2. Нажать кнопку "Поделиться" (квадрат со стрелкой)
3. Выбрать "На экран «Домой»"
4. Нажать "Добавить"

**Результат:**
- Иконка на домашнем экране
- Запуск в полноэкранном режиме
- Без адресной строки Safari
- Как нативное приложение

### Android (Chrome)

1. Открыть в Chrome
2. Нажать меню (три точки)
3. Выбрать "Добавить на главный экран"
4. Подтвердить

**Или автоматически:**
- Chrome покажет баннер "Добавить на главный экран"
- После нескольких посещений

## 🔍 Тестирование

### Chrome DevTools

```bash
# 1. Открыть DevTools (F12)
# 2. Toggle Device Toolbar (Ctrl+Shift+M)
# 3. Выбрать устройство:
   - iPhone 12 Pro
   - iPad Pro
   - Galaxy S21
   - Pixel 5
# 4. Или кастомный размер
```

### Тестовые устройства

**Рекомендуется протестировать:**
- iPhone (iOS Safari)
- Android (Chrome)
- iPad (Safari)
- Android Tablet

### Lighthouse Mobile

```bash
# В Chrome DevTools
# 1. Вкладка Lighthouse
# 2. Выбрать Mobile
# 3. Запустить анализ

# Целевые показатели:
- Performance: >90
- Accessibility: >95
- Best Practices: >90
- SEO: >90
```

## 🐛 Известные проблемы

### iOS Safari

**Проблемы:**
- Input zoom при фокусе (исправлено: font-size 16px)
- 100vh включает адресную строку (используем flex)
- Bounce scroll (отключен: overscroll-behavior)

**Решения применены:**
```css
input, textarea {
  font-size: 16px; /* Предотвращает zoom */
}

body {
  overscroll-behavior-y: contain; /* Без bounce */
}
```

### Android Chrome

**Особенности:**
- Pull-to-refresh может срабатывать
- Virtual keyboard уменьшает viewport
- Dark mode может переопределять цвета

**Решения:**
```html
<meta name="theme-color" content="#0C0F16">
```

## 📊 Производительность

### Мобильные метрики

**Целевые значения:**
- First Contentful Paint: <1.5s
- Largest Contentful Paint: <2.5s
- Time to Interactive: <3.0s
- Cumulative Layout Shift: <0.1

**Оптимизации:**
- Ленивая загрузка изображений
- Code splitting (Vite)
- Tree shaking неиспользуемого кода
- Минификация CSS/JS

### Размер bundle

**Mobile bundle:**
- Main JS: ~500KB (gzipped)
- CSS: ~40KB (gzipped)
- Fonts (KaTeX): ~1.5MB (кешируется)

**Итого:** ~2MB первая загрузка

## ✨ Будущие улучшения

**Планируется:**
- [ ] Offline mode (Service Worker)
- [ ] Синхронизация в фоне
- [ ] Push уведомления
- [ ] Share API для экспорта
- [ ] Haptic feedback
- [ ] Voice input для заметок
- [ ] Camera integration для изображений
- [ ] Gesture navigation (swipe between files)
- [ ] Split screen на iPad
- [ ] Keyboard shortcuts для внешних клавиатур

## 📚 Лучшие практики

### Разработка для мобильных

1. **Mobile-first подход**
   ```css
   /* Сначала мобильные стили */
   .element { ... }

   /* Затем десктопные */
   @media (min-width: 768px) {
     .element { ... }
   }
   ```

2. **Touch-friendly UI**
   - Минимум 44x44px для кликабельных элементов
   - Достаточное расстояние между элементами
   - Визуальная обратная связь при нажатии

3. **Производительность**
   - Избегать тяжелых анимаций
   - Оптимизировать изображения
   - Ленивая загрузка контента
   - Использовать CSS transitions вместо JS

4. **Доступность**
   - Поддержка screen readers
   - Достаточный контраст цветов
   - Читаемые размеры шрифтов
   - Keyboard navigation

## 🎓 Ресурсы

**Документация:**
- [MDN: Mobile Web Development](https://developer.mozilla.org/en-US/docs/Web/Guide/Mobile)
- [Google: Mobile-Friendly Websites](https://developers.google.com/search/mobile-sites)
- [Apple: iOS Web Design](https://developer.apple.com/design/human-interface-guidelines/ios/overview/themes/)

**Тестирование:**
- [BrowserStack](https://www.browserstack.com/) - Тестирование на реальных устройствах
- [Chrome DevTools](https://developer.chrome.com/docs/devtools/) - Device emulation
- [Lighthouse](https://developers.google.com/web/tools/lighthouse) - Performance audit

---

**Статус:** ✅ Полностью адаптивно для мобильных устройств
**PWA:** ✅ Готово к установке на домашний экран
**Производительность:** ✅ Оптимизировано для мобильных сетей
