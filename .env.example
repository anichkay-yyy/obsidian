# Knowledge Base Configuration

# Server Configuration
KB_PORT=33005

# Vault Path (where your markdown files are stored)
KB_VAULT_PATH=./vault

# Static Files Directory (built frontend)
KB_STATIC_DIR=./static

# Authentication
KB_AUTH_USER=admin
KB_AUTH_PASS=changeme

# Usage:
# 1. Copy this file to .env
# 2. Update the values as needed
# 3. Source it before starting: source .env && ./start.sh
